int removeDuplicates(int* nums, int numsSize) {
    int k = 1;//用k来记录不是重复项的数
    for(int i = 1 ; i < numsSize ; i++ ){
        if(nums[i]!=nums[i-1]){
            nums[k] = nums[i];
            k++;//指针i用来遍历数组，指针k用来指向接下来需要改变的数
        }
    }
    return k;
}
数组双指针基础
