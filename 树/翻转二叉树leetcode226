给你一棵二叉树的根节点 root ，翻转这棵二叉树，并返回其根节点
示例 1：
输入：root = [4,2,7,1,3,6,9]
输出：[4,7,2,9,6,3,1]

我的解答（错误❌）
struct TreeNode* invertTree(struct TreeNode* root) {
    if(root!=NULL){                                    错误1：问题 1：未检查左右子节点是否为 NULL（空指针解引用）
        int n = root->left->val;
        root->left->val=root->right->val;              错误2：翻转二叉树 ≠ 交换左右孩子的值！ 正确做法：直接交换左右子树的指针！
        root->right->val=n;
    }
    invertTree(root->left);
    invertTree(root->right);
    return root;
}

修改后
struct TreeNode* invertTree(struct TreeNode* root) {
    if(root==NULL) return NULL;
    struct TreeNode* temp = root->left;
    root->left=root->right;
    root->right=temp;
    invertTree(root->left);
    invertTree(root->right);
    return root;
}
时复o（n）  空复o（h）
